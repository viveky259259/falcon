# Rust
/target/
**/*.rs.bk
# Cargo.lock is committed: falcon ships binaries (falcon/falcon-lsp/falcon-mcp),
# so a locked, reproducible dependency graph is required for CI `--locked` builds
# and `cargo install` from a release tag.

# Dart
.dart_tool/
.packages
build/
pubspec.lock

# IDE
.idea/
.vscode/
*.iml
.cursor/
.claude/

# OS
.DS_Store
Thumbs.db
